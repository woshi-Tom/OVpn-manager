FROM python:3.11-slim

WORKDIR /app

COPY web/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY web/ .

RUN mkdir -p /var/log/vpn-manager

EXPOSE 5000

CMD ["python3", "app.py"]
